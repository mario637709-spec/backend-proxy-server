{
  "name": "ytdown-backend",
  "version": "1.0.0",
  "description": "YouTube Downloader Backend - Optimized for 10K+ users",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node test-redis.js",
    "postinstall": "pip install requests curl_cffi || true && node download-ytdlp.js"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.6",
    "dotenv": "^16.3.1",
    "express": "^4.22.2",
    "express-rate-limit": "^7.5.1",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
